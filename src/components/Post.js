import React from "react";
import { Grid, Text, Butten, Input } from "../elements/Grid";

const Post = () => {
    return (
        <>
            <Grid></Grid>
        </>
    );
};

export default Post;
